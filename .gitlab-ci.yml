stages:
  - install
  - build
  - test

install:
  stage: install
  script:
    - yarn install

build:
  stage: build
  script:
    - yarn build
  artifacts:
    paths:
      - dist/

test:
  stage: test
  script:
    - yarn test
